#include <string>
#include <vector>

using namespace std;

vector<int> solution(vector<vector<int>> users, vector<int> emoticons) {
    vector<int> answer;

    int n_user = users.size();
    int n_em = emoticons.size();

    int discount[4] = {1, 2, 3, 4};
    
    return answer;
}